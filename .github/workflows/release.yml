name: Release
on: 
  push:
    tags:
      - "v*"

jobs:
  release:
    name: "Release"
    runs-on: "ubuntu-latest"
    steps:
      - uses: "romarioOliveira9/release-tag-v1@main"
        with:
          repo_token: "${{ secrets.GITHUB_TOKEN }}"
          prerelease: false